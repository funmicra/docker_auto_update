name: Build & Push to Docker Hub

on:
  push:
    branches: ["dev"]
    tags: ["v*"]

jobs:
  build:
    name: Build and Push Image
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_PASS }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            funmicra/docker_update:latest
            funmicra/docker_update:${{ github.sha }}
            funmicra/docker_update:${{ github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Pull new image
        run: docker pull funmicra/Docker-Update:latest

      - name: Stop old container
        run: |
          docker stop docker-auto-update || true
          docker rm docker-auto-update || true

      - name: Run updated container
        run: |
          docker run -d \
            --name Docker-Update \
            --restart unless-stopped \
            -v /var/run/docker.sock:/var/run/docker.sock \
            -v logs:/app/logs \
            --env-file /opt/auto-update/.env \
            funmicra/docker-auto-update:latest